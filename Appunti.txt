LANCIO DEGLI SCRIPTS: 
npm run db:rest
npm run db:seed:first


------------------------------ COMPLETE-CUSTOMER.HTML------------------------------------------
Il div #categoriesList è il contenitore in cui lo script inserirà dinamicamente le checkbox delle categorie via JS.

Per i ristoranti preferiti:

l’input#restaurantSearch permette di digitare una ricerca;

il div#restaurantSuggestions (inizialmente nascosto con style="display: none;") sarà mostrato e popolato con suggerimenti dal JS (es. dopo fetch all’API ristoranti) e potrà usare componenti di tipo lista (.list-group) per visualizzare suggerimenti. 
getbootstrap.com
+2
Stack Overflow
+2

il div#selectedRestaurants sarà usato per mostrare i ristoranti scelti dall’utente (ad esempio con tag o badge).

Manteniamo lo stesso stile del form precedente per coerenza visiva.
---------------------------------------------------------------------------------